#include "vg.h"

/*
 * Post-processing passes (bloom, persistence, flicker) will live here.
 * Keep this isolated from geometry rendering so look-dev can iterate quickly.
 */
int vg_fx_stub(void) {
    return 0;
}
